# Тест-кейсы NexTalk

Покрытие: 19 flows из [c4-model.md](c4-model.md), функциональные требования FR-1..FR-30 из [README §5](../README.md#5-функциональные-требования-fr).

| UC | Тема | Flows | FR |
|:--|:--|:--|:--|
| UC-1 | Аутентификация | Flow 1, 15 | FR-1, FR-2, FR-3 |
| UC-2 | Управление сервером | Flow 2, 12, 13, 17, 18, 19 | FR-5, FR-6, FR-7, FR-10, FR-11, FR-13, FR-30 |
| UC-3 | Сообщения | Flow 3, 9, 11, 14 | FR-15, FR-16, FR-17, FR-18, FR-19, FR-20 |
| UC-4 | Голосовые каналы | Flow 4, 10 | FR-21, FR-22 |
| UC-5 | Модерация | Flow 5, 6 | FR-12, FR-13, FR-25, FR-26 |
| UC-6 | Онлайн-статусы | Flow 7 | FR-27, FR-28 |

---

# UC-1: Аутентификация

## TC-1.1 Успешная регистрация

**FR:** FR-1, FR-2

**Предусловие:** пользователь не зарегистрирован.

**Шаги:**

1. На SPA нажать кнопку **«Зарегистрироваться»**.
2. Убедиться, что произошел редирект на страницу авторизации.
3. Заполнить регистрационную форму:
   - почта;
   - фио;
   - выбор метода аутентификации;
4. Нажать **«Продолжить»**.
5. Убедиться, что произошел редирект на форму с паролем.
6. Ввести пароль.
4. Нажать **«Продолжить»**.
5. Убедиться, что произошел редирект обратно в SPA.

**Ожидаемый результат:**

- Аккаунт успешно создан в Zitadel.
- Пользователь автоматически авторизован.
- Пользователь перенаправлен на главную страницу приложения.
- В интерфейсе отображаются имя пользователя.

---

## TC-1.2 Регистрация с уже существующим email

**FR:** FR-1

**Предусловие:** пользователь с указанным email уже существует.

**Шаги:**

1. На SPA нажать кнопку **«Зарегистрироваться»**.
2. Убедиться, что произошел редирект на страницу авторизации.
3. Заполнить регистрационную форму:
   - почта(уже созданного пользователя);
   - фио;
   - выбор метода аутентификации;
4. Нажать **«Продолжить»**.
5. Убедиться, что произошел редирект на форму с паролем.
6. Ввести пароль.
4. Нажать **«Продолжить»**.

**Ожидаемый результат:**

- Сообщение об ошибке - пользователь с таким email уже существует.
- Новый аккаунт не создается.
- Редирект обратно в SPA не выполняется.

---

## TC-1.3 Успешный вход

**FR:** FR-2

**Предусловие:** пользователь зарегистрирован.

**Шаги:**

1. На SPA нажать кнопку **«Войти»**.
2. Убедиться, что произошел редирект на страницу ввода логина.
3. Ввести email.
4. Убедиться, что произошел редирект на страницу ввода пароля.
5. Ввести корректный пароль
6. Подтвердить вход.
7. Убедиться, что произошел редирект обратно в SPA.

**Ожидаемый результат:**

- Пользователь успешно авторизован.
- Пользователь перенаправлен на главную страницу приложения.
- В интерфейсе отображаются имя пользователя.

---

## TC-1.4 Вход с неверным паролем

**FR:** FR-2

**Предусловие:** пользователь зарегистрирован.

**Шаги:**

1. На SPA нажать кнопку **«Войти»**.
2. Убедиться, что произошел редирект на страницу ввода логина.
3. Ввести email.
4. Убедиться, что произошел редирект на страницу ввода пароля.
5. Ввести некорректный пароль
6. Подтвердить вход.

**Ожидаемый результат:**

- Ошибка авторизации.
- Пользователь остается на странице входа.
- Редирект обратно в SPA не выполняется.

---

## TC-1.5 Silent Token Refresh

**FR:** FR-3

**Предусловие:** пользователь авторизован, сессия Zitadel активна.

**Шаги:**

1. Выполнить вход в приложение.
2. Дождаться истечения срока действия access token.
3. Продолжить работу в приложении без обновления страницы.

**Ожидаемый результат:**

- Access token автоматически обновляется в фоновом режиме через скрытый iframe.
- Пользователь остается авторизованным.
- Редирект на страницу Zitadel не происходит.
- Текущая сессия не прерывается.

---

# UC-2: Управление сервером

## TC-2.1 Успешное создание сервера

**FR:** FR-5

**Предусловие:** пользователь авторизован.

**Шаги:**

1. Нажать **«Создать сервер»**.
2. Ввести название сервера.
3. Подтвердить создание.

**Ожидаемый результат:**

- Сервер успешно создан.
- Создателю автоматически назначается роль **Owner**.
- Автоматически создается текстовый канал `general`.

---

## TC-2.2 Создание текстового канала

**FR:** FR-6

**Предусловие:** пользователь авторизован и имеет роль **Owner** или **Admin** на сервере, существует сервер.

**Шаги:**

1. Открыть сервер.
2. Нажать **«Добавить канал»** рядом с разделом каналов.
3. Ввести название канала, выбрать тип **text**.
4. Подтвердить создание.

**Ожидаемый результат:**

- Новый текстовый канал отображается в списке каналов у всех участников без перезагрузки страницы.
- Остальные участники получают событие `channel.created` по SignalR.

---

## TC-2.3 Создание голосового канала

**FR:** FR-7

**Предусловие:** пользователь авторизован и имеет роль **Owner** или **Admin** на сервере, существует сервер.

**Шаги:**

1. Открыть сервер.
2. Нажать **«Добавить канал»** рядом с разделом каналов.
3. Ввести название канала, выбрать тип **voice**.
4. Подтвердить создание.

**Ожидаемый результат:**

- Новый голосовой канал отображается в списке каналов у всех участников без перезагрузки страницы.
- Остальные участники получают событие `channel.created` по SignalR.

---

## TC-2.4 Генерация инвайт-ссылки

**FR:** FR-11

**Предусловие:** пользователь авторизован и имеет роль **Owner** или **Admin** на сервере, сервер существует.

**Шаги:**

1. Открыть сервер.
2. Нажать на любой канал.
3. Нажать **«Пригласить»**
4. Нажать **«Создать инвайт»**
5. Указать TTL ссылки (опционально; `null` - бессрочная).
6. Указать лимит использований (опционально; `null` - без лимита).
7. Нажать **«Готово»**.

**Ожидаемый результат:**

- Guild Service возвращает `201 Created` с полями `code`, `expiresAt`, `maxUses`.
- Сгенерирована уникальная инвайт-ссылка с заданными параметрами.
- Ссылка доступна для копирования.

---

## TC-2.5 Переход по валидной инвайт-ссылке

**FR:** FR-12

**Предусловие:** пользователь авторизован, ссылка действующая (не истекла, лимит не исчерпан), пользователь не состоит на сервере и не забанен.

**Шаги:**  

1. На главной странице вставить код приглашения
2. Нажать **«Присоединиться»**

**Ожидаемый результат:**

- Guild Service возвращает `200 OK` с данными сервера.
- Новому участнику назначается роль **Member**.
- Сервер появляется в списке серверов пользователя.
- Онлайн-участники сервера получают событие `member.joined` по SignalR.

---

## TC-2.6 Использование просроченной инвайт-ссылки

**FR:** FR-12

**Предусловие:** пользователь авторизован, срок действия ссылки истек, пользователь не состоит на сервере и не забанен.

**Шаги:**

1. Перейти по просроченной ссылке.

**Ожидаемый результат:**

- Guild Service возвращает ошибку о том, что ссылка недействительна.
- Пользователь не добавляется на сервер.

---

## TC-2.7 Превышен лимит использований инвайта

**FR:** FR-12

**Предусловие:** лимит использований ссылки исчерпан.

**Шаги:**

1. Перейти по ссылке после достижения лимита.

**Ожидаемый результат:**

- Guild Service возвращает ошибку о недействительности ссылки.
- Пользователь не добавляется на сервер.

---

# UC-3: Сообщения

## TC-3.1 Отправка сообщения

**FR:** FR-15, FR-18, FR-19

**Предусловие:** пользователь авторизован и находится в текстовом канале.

**Шаги:**

1. Ввести текст сообщения.
2. Нажать **«Отправить»**.

**Ожидаемый результат:**

- Сообщение сохранено в базе данных.
- Сообщение отображается у отправителя.
- Сообщение доставляется другим онлайн-участникам канала через SignalR (`message.created`).

---

## TC-3.2 Получение сообщения другим пользователем

**FR:** FR-18

**Предусловие:** два пользователя авторизованы и подключены к WebSocket; оба состоят на одном сервере в одном текстовом канале.

**Шаги:**

1. Пользователь A отправляет сообщение в канал.
2. Пользователь B наблюдает за списком сообщений канала.

**Ожидаемый результат:**

- Новое сообщение появляется у пользователя B без перезагрузки страницы.

---

## TC-3.3 Отправка пустого сообщения

**FR:** FR-15

**Предусловие:** пользователь авторизован и находится в текстовом канале.

**Шаги:**

1. Попытаться отправить пустую строку.

**Ожидаемый результат:**

- Сообщение не отправляется.
  
---

## TC-3.4 Потеря соединения WebSocket

**FR:** FR-18

**Предусловие:** пользователь авторизован и подключен к WebSocket.

**Шаги:**

1. Отключить сеть.
2. Попытаться отправить сообщение.

**Ожидаемый результат:**

- Сообщение не отправляется.
- Пользователь получает уведомление об ошибке соединения.

---

# UC-4: Голосовые каналы

## TC-4.1 Подключение к голосовому каналу

**FR:** FR-21

**Предусловие:** пользователь авторизован и состоит на сервере, где есть голосовой канал.

**Шаги:**

1. В списке каналов нажать на голосовой канал → **«Войти»**.

**Ожидаемый результат:**

- Voice Service возвращает `200 OK` с LiveKit-токеном.
- Устанавливается WebRTC-соединение с LiveKit.
- Начинается передача аудио.
- Остальные участники сервера получают событие `voice.joined` по SignalR.

---

## TC-4.2 Отключение от голосового канала

**FR:** FR-22

**Предусловие:** пользователь авторизован и подключен к голосовому каналу.

**Шаги:**

1. Нажать **«Выйти»** из голосового канала.

**Ожидаемый результат:**

- Voice Service возвращает `200 OK`.
- WebRTC-соединение с LiveKit корректно разрывается.
- Остальные участники сервера получают событие `voice.left` по SignalR.

---

## TC-4.3 Нет доступа к микрофону

**FR:** FR-21

**Предусловие:** пользователь авторизован и состоит на сервере с голосовым каналом; доступ к микрофону в браузере запрещен.

**Шаги:**

1. Нажать на голосовой канал → **«Войти»**.

**Ожидаемый результат:**

- Отображается ошибка доступа к микрофону.
- Передача аудио не начинается.

---

## TC-4.4 Потеря соединения во время голосового звонка

**FR:** FR-21, FR-22

**Предусловие:** пользователь авторизован и подключен к голосовому каналу.

**Шаги:**

1. Отключить сеть во время активного голосового соединения.

**Ожидаемый результат:**

- Пользователь отключается от голосового канала.
- Отображается уведомление о потере соединения.

---

# UC-5: Модерация

## TC-5.1 Кик участника

**FR:** FR-25

**Предусловие:** действие выполняет **Owner** или **Admin**; целевой пользователь является участником сервера.

**Шаги:**

1. Выбрать участника сервера.
2. Нажать **«Кик»** и подтвердить.

**Ожидаемый результат:**

- Участник удален из сервера.
- Кикнутый пользователь получает событие `guild.force.disconnect` по SignalR - WS-соединение с гильдией разрывается.
- Если пользователь был в голосовом канале - принудительно отключается от него.
- Остальные участники получают событие `member.left` по SignalR.
- Исключенный пользователь может повторно вступить по действующему инвайту.

---

## TC-5.2 Бан участника и немедленное отключение

**FR:** FR-26

**Предусловие:** действие выполняет **Owner** или **Admin**; целевой пользователь является участником сервера и подключен к WebSocket.

**Шаги:**

1. Выбрать участника сервера → **«Забанить»** → подтвердить.
2. Убедиться, что забаненный пользователь получил принудительное отключение.
3. Попробовать принять инвайт на этот сервер от имени забаненного пользователя.

**Ожидаемый результат:**

- Участник удален из сервера и добавлен в список банов.
- Забаненный пользователь немедленно получает событие `guild.force.disconnect` по SignalR.
- Если пользователь был в голосовом канале - принудительно отключается от него.
- Остальные участники получают событие `member.banned` по SignalR.
- Попытка принять инвайт возвращает `HTTP 403 Forbidden`.
- Инвайт-ссылка не позволяет обойти бан.

---

## TC-5.3 Вступление по инвайту забаненным пользователем

**FR:** FR-12, FR-26

**Предусловие:** пользователь забанен на сервере; инвайт-ссылка на этот сервер действующая.

**Шаги:**

1. Перейти по действующей инвайт-ссылке от имени забаненного пользователя.

**Ожидаемый результат:**

- Guild Service возвращает `HTTP 403 Forbidden`.
- Пользователь не добавляется на сервер.

---

# UC-6: Онлайн-статусы

## TC-6.1 Пользователь отображается онлайн

**FR:** FR-27

**Предусловие:** пользователь авторизован и подключен к WebSocket.

**Шаги:**

1. Пользователь отправляет Heartbeat через SignalR.

**Ожидаемый результат:**

- Статус пользователя отображается как **Online** для участников общих серверов.
- Участники общих серверов получают событие `presence.online` по SignalR (при первом появлении).

---

## TC-6.2 Пользователь становится офлайн

**FR:** FR-28

**Предусловие:** пользователь авторизован, подключен к WebSocket и ранее отправлял Heartbeat.

**Шаги:**

1. Прекратить отправку Heartbeat (или отключить WebSocket-соединение).

**Ожидаемый результат:**

- Через 30 секунд после последнего Heartbeat статус меняется на **Offline**.
- Участники общих серверов получают событие `presence.offline` по SignalR.

---

## TC-6.3 Статус остается онлайн при регулярном Heartbeat

**FR:** FR-27

**Предусловие:** пользователь авторизован и подключен к WebSocket.

**Шаги:**

1. Отправлять Heartbeat каждые 20 секунд в течение 60 секунд.

**Ожидаемый результат:**

- Статус пользователя остается **Online** на протяжении всего теста.

---

# UC-FT: Отказоустойчивость

Тест-кейсы проверяют механизмы fault tolerance: идемпотентность, дедлайны, circuit breaker и retry.
Автоматические тесты: `FaultTolerance/IdempotencyTests.cs`, `FaultTolerance/DeadlineTests.cs`, `FaultTolerance/DeadlineMiddlewareTests.cs`.

---

## TC-FT.1 Идемпотентность создания сообщения (повторный запрос)

**Компонент:** messaging-service `POST /internal/messages`

**Автотест:** `IdempotencyTests.DuplicateKey_SecondRequest_Returns200`, `DuplicateKey_ReturnsSameMessageId`, `DuplicateKey_CreatesOnlyOneMessageInDb`

**Предусловие:** messaging-service запущен, БД доступна.

**Шаги:**

1. Отправить `POST /internal/messages` с заголовком `X-Idempotency-Key: key-1` и телом `{ ChannelId, GuildId, AuthorId, AuthorName, Content }`.
2. Зафиксировать `Id` сообщения из ответа 201.
3. Отправить идентичный запрос с тем же `X-Idempotency-Key: key-1`.

**Ожидаемый результат:**

- Первый запрос → `201 Created`, сообщение сохранено в БД.
- Второй запрос → `200 OK`, тело ответа идентично первому (тот же `Id`).
- В таблице `Messages` ровно одна запись для этой пары `ChannelId`/`GuildId`.

---

## TC-FT.2 Идемпотентность - разные ключи создают разные сообщения

**Компонент:** messaging-service `POST /internal/messages`

**Автотест:** `IdempotencyTests.UniqueKeys_CreateSeparateMessages`

**Предусловие:** messaging-service запущен.

**Шаги:**

1. Отправить `POST /internal/messages` с `X-Idempotency-Key: key-A`.
2. Отправить `POST /internal/messages` с `X-Idempotency-Key: key-B` (то же тело).

**Ожидаемый результат:**

- Оба запроса → `201 Created`.
- В БД две разные записи с разными `Id`.

---

## TC-FT.3 Дедлайн истек - запрос отклоняется с 504

**Компонент:** DeadlineMiddleware (messaging-service, guild-service)

**Автотест:** `DeadlineTests.ExpiredDeadline_Returns504`, `DeadlineMiddlewareTests.ExpiredDeadline_Returns504`

**Предусловие:** сервис запущен.

**Шаги:**

1. Отправить запрос с заголовком `X-Deadline: <timestamp в прошлом>` (например, `UtcNow - 1s` в формате ISO 8601).

**Ожидаемый результат:**

- Ответ `504 Gateway Timeout`.
- Тело: `{ "error": "Request timeout", "retryAfter": 5 }`.
- Обработчик запроса не вызывается.

---

## TC-FT.4 Валидный дедлайн - запрос обрабатывается нормально

**Компонент:** DeadlineMiddleware (messaging-service, guild-service)

**Автотест:** `DeadlineTests.FutureDeadline_ProcessesNormally`, `DeadlineMiddlewareTests.FutureDeadline_ProcessesNormally`

**Предусловие:** сервис запущен.

**Шаги:**

1. Отправить запрос с заголовком `X-Deadline: <UtcNow + 10s>` в ISO 8601.

**Ожидаемый результат:**

- Запрос успешно обработан (200/201 в зависимости от эндпоинта).
- Дедлайн-токен создан, но не сработал.

---

## TC-FT.5 Невалидный формат X-Deadline - заголовок игнорируется

**Компонент:** DeadlineMiddleware

**Автотест:** `DeadlineTests.InvalidDeadlineFormat_Ignored_ProcessesNormally`, `DeadlineMiddlewareTests.InvalidDeadlineFormat_Ignored_ProcessesNormally`

**Предусловие:** сервис запущен.

**Шаги:**

1. Отправить запрос с заголовком `X-Deadline: not-a-date`.

**Ожидаемый результат:**

- Запрос обработан нормально (без 504).
- `DateTimeOffset.TryParse` вернул `false` → middleware пропускает дедлайн.

---

## TC-FT.6 Circuit breaker - открытие после серии ошибок

**Компонент:** Polly ResiliencePipeline (guild-service client, messaging-service client в websocket-gateway)

**Автотест:** ручной / интеграционный (circuit breaker требует минимум 5 запросов и 30-секундного окна)

**Предусловие:** зависимый сервис (guild-service или messaging-service) недоступен.

**Шаги:**

1. Остановить зависимый сервис (`kubectl scale deployment/guild-service --replicas=0`).
2. Отправить 6+ запросов к websocket-gateway, требующих обращения к guild-service.
3. Подождать открытия circuit breaker (50% ошибок за 30s при минимум 5 запросах).
4. Запустить guild-service обратно.
5. Подождать 15 секунд (BreakDuration) и отправить новый запрос.

**Ожидаемый результат:**

- Шаги 2–3: после порога ошибок запросы завершаются мгновенно с ошибкой (circuit open), без ожидания таймаута.
- В логах: событие `Circuit breaker opened guild-service for 15s`.
- Шаг 5: circuit закрывается, запросы снова проходят к guild-service.
- В логах: `Circuit breaker closed guild-service`.

---

## TC-FT.7 Retry - временная ошибка исправляется после повтора

**Компонент:** Polly RetryStrategy (exponential backoff, 3 попытки)

**Автотест:** ручной

**Предусловие:** зависимый сервис возвращает 5xx на первые 2 запроса, затем 200.

**Шаги:**

1. Настроить stub-сервер (или chaos proxy), возвращающий 503 первые 2 раза, затем 200.
2. Отправить один запрос через websocket-gateway к зависимому сервису.

**Ожидаемый результат:**

- В логах: `Retry 1/3 guild-service: 503`, `Retry 2/3 guild-service: 503`.
- Третья попытка успешна - запрос завершается с ожидаемым результатом.
- Общее время: ~200ms + ~400ms (exponential) = ~600ms overhead.
